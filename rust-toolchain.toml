# Pinned to match the bbx_audio workspace toolchain (bbx_daisy 0.5 is edition-2024,
# Rust >= 1.89). Keeps builds reproducible with upstream; update both repos together.
[toolchain]
channel = "nightly-2025-06-08"
components = ["clippy", "rustfmt"]
targets = ["thumbv7em-none-eabihf"]
